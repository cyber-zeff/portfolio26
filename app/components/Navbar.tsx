export default function Navbar() {
    return (
        <nav>
            {/* logo */}
            <div></div>

            {/* links -> chatbot, projs, experience */}
            <div></div>

            {/* some btns -> download resume */}
        </nav>
    );
}